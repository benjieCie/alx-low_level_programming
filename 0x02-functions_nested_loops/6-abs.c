#include "main.h"

/**
 * _abs - calculate the asbolute value
 *
 * @n: number as integer
 *
 * Return: absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
